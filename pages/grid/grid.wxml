<view class="grids">
  <navigator class="item" wx:for="{{cars}}" wx:key="id"
             url="/pages/gridDetail/gridDetail?id={{item.id}}&name={{item.name}}&sinaUrl={{item.sinaUrl}}">
    <image src="{{item.sinaUrl}}" mode='scaleToFill'/>
  </navigator>
</view>


